// conf.js — 常量/选择器/时序
(function (global) {
  'use strict';
  global.GPTB = global.GPTB || {};
  GPTB.conf = {
    PANEL_ID: 'gptb-mini-panel',
    // 先只存 UI 相关；后面再加 EDITOR_SEL、超时常量等
  };
})(typeof window !== 'undefined' ? window : this);

  NS.Config = Config;

})(typeof window !== 'undefined' ? window : this);
